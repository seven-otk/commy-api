#!/usr/bin/env groovy

node {
    def images = [:]
    def imageName = "7otk/commy-api"
    def scmVars = null

    stage('prerequisite') {
        scmVars = checkout(scm)
    }

    stage('build') {

    sh 'whoami'
    }
}